<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="8dp">

    <EditText
        android:id="@+id/edit_remind"
        android:layout_width="275dp"
        android:layout_height="wrap_content"
        android:hint="Type reminder"
        app:layout_constraintEnd_toStartOf="@+id/add_reminder"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ADD"
            android:id="@+id/add_reminder"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

    <ScrollView
        android:layout_width="363dp"
        android:layout_height="627dp"
        app:layout_constraintStart_toStartOf="@id/edit_remind"
        app:layout_constraintTop_toBottomOf="@id/edit_remind"
        android:layout_marginTop="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:id="@+id/myLinearLayout"/>
    </ScrollView>


</androidx.constraintlayout.widget.ConstraintLayout>